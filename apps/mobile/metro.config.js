// Metro config for the Expo Router mobile app. Uses the default Expo
// preset — nothing custom needed for the current dependency graph.
const { getDefaultConfig } = require('expo/metro-config');

module.exports = getDefaultConfig(__dirname);
